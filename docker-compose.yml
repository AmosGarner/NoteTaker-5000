version: "3"

services:
  backend:
    build: ./backend/
    volumes:
      - ./backend:/backend
    working_dir: /backend
    ports:
      - 3000:3000
    command: npm run dev
  
  # client-angular:
  #   hostname: localhost
  #   build: ./client-angular
  #   ports:
  #     - 80:80
